import { ComingSoon } from '@/components/common/ComingSoon';

export default function ProfilePage() {
  return (
    <ComingSoon
      sectionName="Profile"
      description="Your warrior profile — streaks, rank history, badges, and squad affiliations."
    />
  );
}
