import { ComingSoon } from '@/components/common/ComingSoon';

export default function SquadsPage() {
  return (
    <ComingSoon
      sectionName="Squads"
      description="Build your team, track group streaks, and hold each other accountable every day."
    />
  );
}
